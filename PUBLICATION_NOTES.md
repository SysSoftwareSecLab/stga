# GitHub publication provenance

Repository: https://github.com/Sunuywq/Stress-Testing

Publication preparation date: 2026-09-18. Analysis baseline: r29. Documentation source: r29-docs1. Manuscript r7/r8 preserves the r6 analyses and artwork while updating availability information.

## Source integrity

The source archive used to prepare this repository has SHA-256:

```text
54b0bd87bb8b93865c03e0aa419776e7c88c6b8e1ec0178c06cf17ec96ef21e5
```

All Python analysis, plotting, setup and verification scripts, shell/PowerShell entry points, dependency locks, pinned input metadata and EXPECTED_RESULTS.json remain byte-for-byte identical to that archive. Publication changes consist of English documentation, root-level license/citation metadata, repository URLs, ignore rules, byte-preserving Git attributes and a repository-layout manifest. The original dated archives are retained separately.

MANIFEST.json records the current repository files and the original source archive hash. It excludes itself to avoid a recursive checksum. The executable baseline remains r29; Git commit links identify the exact public snapshot.

## Validation evidence

The clean Linux r29 run reviewed on 2026-09-04 downloaded and verified the fixed inputs, completed all 11 analysis steps, and passed checks for 30 outputs. All 23 structured outputs and four manuscript panel PDFs matched their reference bytes. The returned full-run archive hash was:

```text
b9bbe2b8ced8ac5935e1bf0a2b1c4999128c88a17bfe8413ea1db3e7d4fc6149
```

This records the reviewed user-supplied logs and outputs. It does not claim independent monitoring of execution or behavioral revalidation of the upstream PatchDiff labels. GitHub preparation did not rerun statistical analyses.

## Public contents

The repository distributes downstream code under the existing MIT License. The upstream artifact is retrieved from its pinned public source by setup and is subject to its own terms. Generated results, environments, caches, private run logs and the manuscript are excluded. EXPECTED_RESULTS.json supplies frozen verification metadata and is deliberately versioned.

English is used for the public guides. Earlier documentation history is retained in DOCUMENTATION_CHANGELOG.md. Its references to the original bilingual archive describe that historical edition; the Chinese guide is not included in this repository.
